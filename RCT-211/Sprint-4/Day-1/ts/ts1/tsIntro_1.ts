let fullName: string;

let age: number;

let isFetching: boolean;

let arr: number[];

let arrStr: Array<string>;

let tuple: [number, string];

enum user {
    User,
    SuperUser,
    Admin,
    SuperAdmin,
}

function product(x: number, y: number) {
    return x*y;
}
function divide(x: number, y: number) {
    return x/y;
}
function isName(x: string): void {
    console.log(x)
}



